name: Build Nothing OS 4.1 Kernel (SUSFS)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-kernel:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Maximize Build Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          swap-size-mb: 8192
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'

      - name: 2. Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential bc curl git python3 python-is-python3 libssl-dev repo flex bison zip unzip

      - name: 3. Setup Kleaf / Bazel Build Workspace Environment
        run: |
          mkdir -p workspace
          cd workspace
          echo ">> Initializing Android 15-6.6 manifest environment wrapper..."
          repo init -u https://android.googlesource.com/kernel/manifest -b common-android15-6.6 --depth=1
          
          echo ">> Syncing manifest environment..."
          repo sync -c -j$(nproc) --no-tags --no-clone-bundle
          
          echo ">> Constructing Kleaf workspace configuration..."
          # Copy the workspace template instead of symlinking so we can append private OEM overrides
          cp build/kernel/kleaf/WORKSPACE WORKSPACE
          chmod +w WORKSPACE
          echo 'local_repository(name = "nt_project", path = "nt_project")' >> WORKSPACE
          
          mkdir -p tools
          ln -sf ../build/kernel/kleaf/bazel.sh tools/bazel

      - name: 4. Checkout Nothing Kernel Source (NOS 4.1)
        uses: actions/checkout@v4
        with:
          repository: NothingOSS/android_kernel_msm-6.6_nothing_sm8735
          ref: sm8735/b/mr
          path: workspace/msm-kernel

      - name: 5. Inject KernelSU-Next & SUSFS
        working-directory: workspace/msm-kernel
        run: |
          echo ">> Downloading KernelSU-Next..."
          curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next/kernel/setup.sh" | bash -

          echo ">> Downloading SUSFS Patches..."
          git clone https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android15-6.6 /tmp/susfs4ksu

          echo ">> Applying SUSFS to Kernel Source..."
          cp -r /tmp/susfs4ksu/kernel_patches/fs/* fs/
          cp -r /tmp/susfs4ksu/kernel_patches/include/linux/* include/linux/
          
          if ! grep -q "susfs.o" fs/Makefile; then
            echo "obj-y += susfs.o" >> fs/Makefile
          fi

          echo ">> Patching KernelSU-Next for SUSFS..."
          cd KernelSU-Next
          PATCH_FILE=$(find /tmp/susfs4ksu/ksu_patches/ -name "*susfs*ksunext*.patch" -o -name "*50_add_susfs_in_ksunext*" | head -n 1)
          if [ -n "$PATCH_FILE" ]; then
            patch -p1 < "$PATCH_FILE" || echo "Patch applied with minor offsets."
          else
            patch -p1 < /tmp/susfs4ksu/ksu_patches/50_add_susfs_in_ksunext-6.6.patch || echo "Fallback patch completed."
          fi
          cd ..

          echo ">> Forcing Config Flags into all target build defconfigs..."
          find arch/arm64/configs/ -type f \( -name "*_defconfig" -o -name "*.config" -o -name "*.fragment" \) -exec sh -c 'echo "\nCONFIG_KSU=y\nCONFIG_SUSFS=y" >> "$1"' _ {} \;

      - name: 5.5 Auto-Mock Nothing OS Private Dependencies
        working-directory: workspace
        run: |
          echo ">> Mapping out hidden OEM Bazel components..."
          cat << 'EOF' > mock_nt.py
          import os
          import re

          os.makedirs("nt_project", exist_ok=True)
          with open("nt_project/BUILD.bazel", "w") as f:
              f.write("")

          symbols = {}
          
          # Recursively scan all Bazel architecture rules in the kernel to locate variables pulled from @nt_project
          for root, dirs, files in os.walk("msm-kernel"):
              for file in files:
                  if file.endswith(".bzl") or file in ["BUILD", "BUILD.bazel"]:
                      fn = os.path.join(root, file)
                      try:
                          with open(fn, "r", encoding="utf-8", errors="ignore") as f:
                              content = f.read()
                          if "nt_project" in content:
                              print(f"[*] Found private OEM hook inside: {fn}")
                              
                              # Extract variable names being requested
                              matches = re.findall(r"load\s*\(\s*[\"\']@nt_project//:dict\.bzl[\"\']\s*,\s*([^)]+)\)", content)
                              for match in matches:
                                  for part in match.split(","):
                                      part = part.strip()
                                      if not part: continue
                                      if "=" in part:
                                          sym = part.split("=")[1].strip().strip("\"").strip("'")
                                      else:
                                          sym = part.strip("\"").strip("'")
                                      if sym:
                                          # Automatically guess expected data-type constraint (Lists vs Dicts)
                                          if "module" in sym.lower() or "out" in sym.lower() or "+" in content:
                                              symbols[sym] = "[]"
                                          else:
                                              # Fallback dict maps core targets such as Metroid (Phone 3 / sm8735 platform configuration)
                                              symbols[sym] = '{"metroid": {}, "sun": {}, "pacman": {}, "pong": {}, "spock": {}, "default": {}}'
                      except Exception:
                          pass

          print(f">> Successfully isolated symbols to fulfill: {list(symbols.keys())}")
          with open("nt_project/dict.bzl", "w") as f:
              for sym, val in symbols.items():
                  f.write(f"{sym} = {val}\n")
          EOF
          python3 mock_nt.py

      - name: 6. Compile the Kernel via Qualcomm Kleaf
        working-directory: workspace
        run: |
          echo ">> Commencing Kernel Compilation via Qualcomm Bazel..."
          tools/bazel run //msm-kernel:kernel_aarch64_dist -- --dist_dir=out_dist

      - name: 7. Upload Boot Image
        uses: actions/upload-artifact@v4
        with:
          name: boot-img-nos4.1-susfs
          path: workspace/out_dist/boot.img
          compression-level: 9
